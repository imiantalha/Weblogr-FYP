function confirmDelete(){return confirm('Are you sure you want to delete this post?');}
function confirmLogout(){return confirm('Are you sure you want to logout?');}
function likeBlog(blogId,csrfToken,button){const body=new URLSearchParams({blog_id:String(blogId),csrf_token:csrfToken});if(button)button.disabled=true;fetch('../comments/likes.php',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded; charset=UTF-8','X-Requested-With':'XMLHttpRequest'},body:body.toString()}).then(async response=>{const data=await response.json();if(!response.ok||!data.success)throw new Error(data.message||'Unable to like the post.');const count=document.getElementById('like-count-'+blogId);if(count)count.textContent=data.likes;if(button){button.classList.add('liked');button.setAttribute('aria-label','Post liked');}}).catch(error=>console.error(error.message)).finally(()=>{if(button)button.disabled=false;});}
